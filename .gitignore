# Secrets — NEVER commit
.env
.env.local
*.pem
*-token.json
google-credentials.json

# Dependencies / build
node_modules/
dist/

# Runtime data — state, not source
data/

# OS / editor
.DS_Store
*.log
npm-debug.log*
